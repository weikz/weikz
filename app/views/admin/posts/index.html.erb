<table style="width:100%">
  <tr>
    <th>文章名稱</th>
    <th>編輯</th>
    <th>刪除</th>
  </tr>
  <% @posts.each do |post| %>
    <tr>
      <td><%=post.title %></td>
      <td><%=link_to("編輯", edit_admin_post_path(post)) %></td>
      <td><%=link_to("刪除", "#") %></td>
    </tr>
  <% end %>
</table>
